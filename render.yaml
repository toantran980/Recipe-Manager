services:
  - type: web
    name: recipe-manager
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: SPRING_DATA_MONGODB_URI
        value: mongodb://localhost:27017/recipe
      - key: JWT_SECRET
        generateValue: true
